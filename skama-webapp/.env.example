# Leave empty in development to use the Vite proxy (see vite.config.ts).
# For production, set the full API URL, e.g. https://api.skama.example
VITE_API_BASE_URL=

# Production deploy: add your frontend origin to skama-api appsettings.json
# Cors:AllowedOrigins (e.g. https://app.skama.example).
