@model ProgressViewModel

<div class="sk-progress" aria-label="@Model.Label" aria-valuemin="0" aria-valuemax="100" aria-valuenow="@Model.Value" role="progressbar">
    <span style="--progress-value:@Model.Value%"></span>
</div>
