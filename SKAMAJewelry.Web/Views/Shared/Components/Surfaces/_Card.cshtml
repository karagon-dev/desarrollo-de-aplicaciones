@model CardViewModel

<article class="sk-card">
    @if (!string.IsNullOrWhiteSpace(Model.Eyebrow))
    {
        <p class="sk-card__eyebrow">@Model.Eyebrow</p>
    }
    <h3>@Model.Title</h3>
    <p>@Model.Description</p>
</article>
